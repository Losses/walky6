diff --git a/src/import.rs b/src/import.rs
index 516aa52..fda3088 100644
--- a/src/import.rs
+++ b/src/import.rs
@@ -4,7 +4,7 @@ use smol::{
     fs::File,
     io::{AsyncBufReadExt, BufReader},
 };
-use std::{collections::BTreeSet, path::PathBuf};
+use std::{collections::BTreeSet, path::PathBuf, sync::Arc, sync::atomic::{AtomicU64, Ordering}};
 use ufotofu::{
     ExpectedFinalError, ProduceAtLeastError,
     codec::{Blame, DecodeError},
@@ -40,14 +40,46 @@ pub enum ImportMode {
     Familiar,
 }
 
+fn write_progress_file(path: &str, phase: &str, processed: u64, total: u64, message: &str) {
+    let json = format!(
+        "{{\"phase\":\"{}\",\"processed\":{},\"total\":{},\"message\":\"{}\"}}",
+        phase, processed, total, message
+    );
+    let _ = std::fs::write(path, &json);
+}
+
 pub async fn import_sneak(args: &ImportArgs) -> Result<()> {
     let file = File::open(&args.src).await?;
+    let file_size = file.metadata().await.map(|m| m.len()).unwrap_or(0);
     let drop_producer = reader_to_bulk_producer(file, new_unbounded_elastic());
     let sneakerweb_fs_path = sneakerweb_dir().await?;
     let namespace_id = NamespaceId::from_bytes(&SNEAKERWEB_NAMESPACE_ID_BYTES);
 
     let mut store = PersistentStore::new(&sneakerweb_fs_path).await?;
 
+    let progress_file = std::env::var("IMPORT_PROGRESS_FILE").ok();
+    let decoded_bytes = Arc::new(AtomicU64::new(0));
+
+    if let Some(ref pf) = progress_file {
+        write_progress_file(pf, "decoding", 0, file_size, "Starting import...");
+    }
+
+    let progress_file_clone = progress_file.clone();
+    let decoded_bytes_clone = decoded_bytes.clone();
+    let progress_handle = smol::spawn(async move {
+        loop {
+            smol::Timer::after(std::time::Duration::from_millis(200)).await;
+            let current = decoded_bytes_clone.load(Ordering::Relaxed);
+            if current == u64::MAX {
+                break;
+            }
+            if let Some(ref pf) = progress_file_clone {
+                write_progress_file(pf, "decoding", current, file_size, "Decoding entries...");
+            }
+        }
+    });
+    progress_handle.detach();
+
     let allowed = match args.mode.clone().unwrap_or_default() {
         ImportMode::All => None,
         ImportMode::Familiar => {
@@ -96,14 +128,19 @@ pub async fn import_sneak(args: &ImportArgs) -> Result<()> {
                         return Ok(Right(()));
                     };
 
+                    let entry_size = metadata.expected_bytes();
                     let mut skip_reason = "blocked";
                     let domain_id = metadata.entry().subspace_id();
 
                     if !blocked.contains(domain_id) {
                         match &allowed {
-                            None => return Ok(Left((metadata, slice_stream))),
+                            None => {
+                                decoded_bytes.fetch_add(entry_size, Ordering::Relaxed);
+                                return Ok(Left((metadata, slice_stream)));
+                            }
                             Some(known) => {
                                 if known.contains(domain_id) {
+                                    decoded_bytes.fetch_add(entry_size, Ordering::Relaxed);
                                     return Ok(Left((metadata, slice_stream)));
                                 }
                                 skip_reason = "unfamiliar";
@@ -121,9 +158,10 @@ pub async fn import_sneak(args: &ImportArgs) -> Result<()> {
 
                     skipped.insert(domain_id.clone());
 
+                    let skip_size = metadata.expected_bytes();
                     slice_stream
                         .skip(
-                            metadata.expected_bytes().try_into().context(
+                            skip_size.try_into().context(
                                 "an entry in the .snk file was too large for this device",
                             )?,
                         )
@@ -132,6 +170,7 @@ pub async fn import_sneak(args: &ImportArgs) -> Result<()> {
                             Ok(()) => anyhow!("unexpected end of .snk file"),
                             Err(err) => map_decode_err(err),
                         })?;
+                    decoded_bytes.fetch_add(skip_size, Ordering::Relaxed);
                     slice_stream
                         .produce_final()
                         .await
@@ -145,6 +184,11 @@ pub async fn import_sneak(args: &ImportArgs) -> Result<()> {
             },
         );
 
+    if let Some(ref pf) = progress_file {
+        write_progress_file(pf, "importing", file_size, file_size, "Importing entries...");
+    }
+    decoded_bytes.store(u64::MAX, Ordering::Relaxed);
+
     import_drop(&mut store, &mut filtering_decoder, true)
         .await
         .map_err(|err| match err {
@@ -169,7 +213,12 @@ pub async fn import_sneak(args: &ImportArgs) -> Result<()> {
         })?;
 
     match store.flush().await {
-        Ok(_) => Ok(()),
+        Ok(_) => {
+            if let Some(ref pf) = progress_file {
+                write_progress_file(pf, "done", file_size, file_size, "Import complete");
+            }
+            Ok(())
+        }
         Err(_) => bail!("failed to flush changes to the sneakerweb store"),
     }
 }
diff --git a/src/lib.rs b/src/lib.rs
new file mode 100644
index 0000000..33026f9
--- /dev/null
+++ b/src/lib.rs
@@ -0,0 +1,9 @@
+pub mod block;
+pub mod commands;
+pub mod domain;
+pub mod export;
+pub mod import;
+pub mod publish;
+pub mod serve;
+pub mod util;
+pub mod logo;
diff --git a/src/main.rs b/src/main.rs
index 8dddd90..29bf385 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -71,8 +71,9 @@ fn main() {
                 })
             }
             &Commands::Serve => {
+                let port = util::sneakerweb_port();
                 smol::block_on(async {
-                    match serve::start_server().await {
+                    match serve::start_server(port).await {
                         Ok(()) => (), // I don't think we have anything interesting to say when the server shuts down?
                         Err(err) => oh_no(&format!("{}", err)).await,
                     }
diff --git a/src/serve.rs b/src/serve.rs
index fd4597a..a213814 100644
--- a/src/serve.rs
+++ b/src/serve.rs
@@ -37,12 +37,12 @@ use crate::util::sneakerweb_dir;
 use crate::util::yay;
 use crate::util::{SNEAKERWEB_NAMESPACE_ID_BYTES, get_domain};
 
-pub async fn start_server() -> Result<()> {
-    // Okay so I started with the tutorial at the end of the Rust book, okay? Got a problem?
-    let listener = TcpListener::bind("0.0.0.0:1312").await.context("failed to bind port 1312 to serve the sneakerweb. Is it in use, or do you need to set permissions?")?;
+pub async fn start_server(port: u16) -> Result<()> {
+    let addr = format!("127.0.0.1:{port}");
+    let listener = TcpListener::bind(&addr).await.context(format!("failed to bind {addr} to serve the sneakerweb. Is it in use, or do you need to set permissions?"))?;
     yay(&format!(
         "serving your sneakerweb at {}",
-        emph_style("http://sneakerweb.localhost:1312/")
+        emph_style(&format!("http://sneakerweb.localhost:{port}/"))
     ))
     .await;
 
@@ -50,11 +50,8 @@ pub async fn start_server() -> Result<()> {
 
     while let Some(stream) = incoming.next().await {
         let mut stream = stream?;
-        match handle_connection(stream.clone()).await {
+        match handle_connection(stream.clone(), port).await {
             Err(err) => {
-                // TODO: There are kinds of problem which we might have which are not well described
-                // by 404 errors. I am going to stick my fingers in my ears and ignore them until things
-                // work a little better.
                 stream
                     .write_all(format!("HTTP/1.1 404 NOT FOUND\r\n\r\n{err}\r\n").as_bytes())
                     .await?;
@@ -80,7 +77,7 @@ enum Domain {
 
 const FRONT_CSS: &str = include_str!("frontpage.css");
 
-async fn handle_connection(stream: TcpStream) -> Result<()> {
+async fn handle_connection(stream: TcpStream, port: u16) -> Result<()> {
     let mut buf_reader = BufReader::new(stream.clone());
     let mut headers = [httparse::EMPTY_HEADER; 64];
     let mut req = httparse::Request::new(&mut headers);
@@ -89,18 +86,13 @@ async fn handle_connection(stream: TcpStream) -> Result<()> {
 
     req.parse(buf_reader.buffer())?;
 
-    // Any request to *.localhost ends up here
-    // which means that someone could do *.*.localhost
-    // but we are expecting EITHER
-    // sneakerweb.localhost
-    // or <b16subspaceid>.localhost
-    // We should error on anything else!
-
     let mut domain = Domain::Homepage(FrontPageOrder::Domain);
     let mut parsed_hostname = false;
 
     let mut hostname = "sneakerweb".to_string();
 
+    let expected_tld = format!("localhost:{port}");
+
     for header in req.headers {
         if header.name == "Host" {
             let hostname_str = std::str::from_utf8(header.value).unwrap();
@@ -119,19 +111,11 @@ async fn handle_connection(stream: TcpStream) -> Result<()> {
                 hostname = host_fst.to_string();
             } else if host_fst != "sneakerweb" {
                 bail!("hostname {} wasn't a Subspace ID or the homepage", host_fst)
-            } else {
-                match req.path {
-                    Some("/oldest") => domain = Domain::Homepage(FrontPageOrder::Oldest),
-                    Some("/newest") => domain = Domain::Homepage(FrontPageOrder::Newest),
-                    Some("/sneakiest") => domain = Domain::Homepage(FrontPageOrder::Sneakiest),
-                    _ => {}
-                }
             }
 
             let tld = host_iterator.next();
-            // TODO: Perhaps something nicer for accounting for the port number in the domain?
-            let Some("localhost:1312") = tld else {
-                bail!("TLD {tld:?} was not .localhost:1312")
+            if tld != Some(&expected_tld) {
+                bail!("TLD {tld:?} was not .localhost:{port}")
             };
 
             if let Some(_whatever) = host_iterator.next() {
@@ -140,17 +124,26 @@ async fn handle_connection(stream: TcpStream) -> Result<()> {
 
             parsed_hostname = true;
         }
-
-        // What other header should we consider?
     }
 
     if !parsed_hostname {
         bail!("did not find a Host header in the request")
     }
 
+    if let Domain::Homepage(_) = &domain {
+        let order = match req.path {
+            Some("/oldest") => FrontPageOrder::Oldest,
+            Some("/newest") => FrontPageOrder::Newest,
+            Some("/sneakiest") => FrontPageOrder::Sneakiest,
+            _ => FrontPageOrder::Domain,
+        };
+        domain = Domain::Homepage(order);
+    }
+
     let mut url_to_parse = "http://".to_string();
     url_to_parse.push_str(&hostname);
-    url_to_parse.push_str(".localhost:1312");
+    url_to_parse.push_str(".localhost:");
+    url_to_parse.push_str(&port.to_string());
     if let Some(path) = req.path {
         url_to_parse.push_str(path);
     }
@@ -169,7 +162,7 @@ async fn handle_connection(stream: TcpStream) -> Result<()> {
                 .await
                 .context("could not open sneakerweb storage")?;
 
-            render_frontpage(&mut store, order, &mut buffer).await?;
+            render_frontpage(&mut store, order, &mut buffer, port).await?;
 
             let mut http_consumer =
                 writer_to_bulk_consumer(stream.clone(), new_unbounded_elastic());
@@ -304,6 +297,7 @@ async fn handle_connection(stream: TcpStream) -> Result<()> {
 
 struct DomainListing {
     id: String,
+    port: u16,
     last_updated: Timestamp,
     has_index: bool,
     has_preview: bool,
@@ -336,7 +330,7 @@ impl DomainListing {
     }
 
     fn root_url(&self) -> String {
-        format!("http://{}.localhost:1312", self.id)
+        format!("http://{}.localhost:{}", self.id, self.port)
     }
 
     fn preview_url(&self) -> Option<String> {
@@ -407,6 +401,7 @@ async fn render_frontpage(
     store: &mut PersistentStore,
     order: FrontPageOrder,
     buffer: &mut Buffer,
+    port: u16,
 ) -> Result<()> {
     let namespace = NamespaceId::from_bytes(&SNEAKERWEB_NAMESPACE_ID_BYTES);
 
@@ -480,6 +475,7 @@ async fn render_frontpage(
         if entry_count >= 2 {
             let listing = DomainListing {
                 id: b16_id,
+                port,
                 last_updated,
                 has_index: index_entry.is_some(),
                 has_preview: sneakerweb_entry.is_some(),
diff --git a/src/util.rs b/src/util.rs
index 4406952..02fa524 100644
--- a/src/util.rs
+++ b/src/util.rs
@@ -31,6 +31,12 @@ where
 
 /// Retrieves the path within the user's home directory where sneakerweb configuration and data are located.
 pub async fn sneakerweb_dir() -> Result<PathBuf, Error> {
+    if let Ok(env_dir) = std::env::var("SNEAKERWEB_DIR") {
+        let dir = PathBuf::from(env_dir);
+        create_dir_all(&dir).await?;
+        return Ok(dir);
+    }
+
     let mut store_path = home_dir().ok_or(Error::new(
         ErrorKind::NotFound,
         "could not determine user home directory",
@@ -41,6 +47,13 @@ pub async fn sneakerweb_dir() -> Result<PathBuf, Error> {
     Ok(store_path)
 }
 
+pub fn sneakerweb_port() -> u16 {
+    std::env::var("PORT")
+        .ok()
+        .and_then(|p| p.parse().ok())
+        .unwrap_or(1312)
+}
+
 /// Styles text using the conventions for sneakerweb domains.
 pub fn domain_style(message: &str) -> String {
     format!("{}", message.bold().color(Color::Green))
